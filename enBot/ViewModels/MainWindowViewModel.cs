namespace enBot.ViewModels;

// MainWindow is not shown on startup — the tray icon is the app shell.
// This ViewModel is kept for Avalonia template compatibility.
public partial class MainWindowViewModel : ViewModelBase
{
}
